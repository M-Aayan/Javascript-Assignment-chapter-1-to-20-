var a = 10, b = 20, c = 30;

// legal variable 
var myName, _user, $price, user1, camelCase;


// Illegal variable names (These will cause errors if uncommented) i got from chtgpt
    // var 1name;       // Starts with a number
    // var @value;      // Uses invalid character (@)
    // var user-name;   // Hyphens not allowed
    // var var;         // JS keyword
    // var alert;       // Reserved function name